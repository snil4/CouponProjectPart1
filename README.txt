Make sure to run the "CouponSchema.sql" script int the "resources" folder
before running the Java project.

Make sure to change the database parameters inside "date/ConnectionPool.java" depending on your SQL connection.